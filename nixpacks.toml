providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = [
  "cd cms && python -m ensurepip --upgrade",
  "cd cms && python -m pip install --upgrade pip",
  "cd cms && python -m pip install -r requirements.txt"
]

[start]
cmd = "cd cms && bash start.sh"
